<%= form_for @patient, :html => { class:  'form-horizontal' } do |f| %>
<% if @patient.errors.any? %>
  <div class="alert">
    <h2><%= pluralize(@patient.errors.count, "error") %> prohibited this patient from being saved:</h2>
    <ul>
    <% @patient.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="control-group">
	<%= f.label :first_name, class:  'control-label' %>
	<div class="controls">
		<%= f.text_field :first_name, class:  'text_field' %>
	</div>
</div>
<div class="control-group">
	<%= f.label :middle_name, class:  'control-label' %>
	<div class="controls">
		<%= f.text_field :middle_name, class:  'text_field' %>
	</div>
</div>
<div class="control-group">
	<%= f.label :last_name, class:  'control-label' %>
	<div class="controls">
		<%= f.text_field :last_name, class:  'text_field' %>
	</div>
</div>

  <div class="control-group">
    <%= f.label :date_of_birth, class:  'control-label' %>
    <div class="controls">
      <%= f.date_field :date_of_birth, class: "datepicker", data: {"date-format" => "yyyy-mm-dd"} %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :sex, class:  'control-label' %>
    <div class="controls">
      <%#= f.text_field :gender, class: 'text_field' %>
      <%= f.select :gender, VALID_GENDER, class:  'select_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :phone, class:  'control-label' %>
    <div class="controls">
      <%= f.telephone_field :phone, class: 'phone_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :externalid, class:  'control-label' %>
    <div class="controls">
      <%= f.text_field :externalid, class: 'text_field' %>
    </div>
  </div>
	<div class="control-group">
		<% if @patient.reg_answers.blank? %>
			<p>This is not registration information available for this patient as yet.
			</p>
		<% else %>
		<div class="control-group">
			<%= f.fields_for :reg_answers do |ans| %>
				<strong>Question :</strong>
		    <div class="controls">
				<%= ans.hidden_field :reg_question_id %>
				<%= ans.text_field :reg_question_text, disabled: true, rows: 2, class: "input-xxlarge" %><br />
			</div>
				<strong>Answer:   </strong>
				<div class="control-group">
		    <div class="controls">
				<%= ans.text_field :content %><br />
			</div>
			</div>
			<% end %>
		</div>
		
		<% end%>
  </div>
  <div class="form-actions">
    <%= f.submit nil, class:  'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                patients_path, class:  'btn' %>
  </div>
	
	
<% end %>
