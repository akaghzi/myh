<%= form_for(@appointment_slot) do |f| %>
  <% if @appointment_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appointment_slot.errors.count, "error") %> prohibited this appointment_slot from being saved:</h2>

      <ul>
      <% @appointment_slot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
