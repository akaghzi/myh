<%= form_for(@gynecology_problem) do |f| %>
  <% if @gynecology_problem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gynecology_problem.errors.count, "error") %> prohibited this gynecology_problem from being saved:</h2>

      <ul>
      <% @gynecology_problem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
